apiVersion: apps/v1
kind: Deployment
metadata:
  name: tech-challenge-production-deployment
spec:
  selector:
    matchLabels:
      app: tech-challenge-production-deployment
  template:
    metadata:
      labels:
        app: tech-challenge-production-deployment
    spec:
      containers:
      - name: tech-challenge-production
        image:  diname/tech-challenge-production:latest
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: tech-challenge-production-general-settings
        - secretRef:
            name: tech-challenge-production-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 180
          periodSeconds: 60
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            cpu: 250m
            memory: 100Mi
          limits:
            memory: 400Mi
            cpu: 500m # 1/2 núcleo
        ports:
        - containerPort: 3004
